#include<stdio.h>
int main()
{
	int a,n;
	printf("enter height:");
	scanf("%d",&a);
	if(a>6)
	printf("the height is taller");
	if(a<=3)
	printf("the height is dwarf");
	if(a>3;a<=6)
	printf("the height is average");
	return 0;
}